<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="device"
            type="com.ks.trackmytag.data.Device" />
        <variable
            name="deviceStates"
            type="com.ks.trackmytag.data.DeviceStates" />
        <variable
            name="clickListener"
            type="com.ks.trackmytag.ui.adapters.DeviceClickListener" />
    </data>

    <com.google.android.material.card.MaterialCardView
        app:cardBackgroundColor="@color/light_gray"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/margin_medium"
        android:layout_marginEnd="@dimen/margin_medium"
        android:layout_marginTop="@dimen/margin_medium">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/margin_medium">

            <TextView
                android:id="@+id/connection_state"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                setStateText="@{deviceStates.connectionStates.get(device.address)}"
                app:layout_constraintBottom_toBottomOf="@+id/connect_button"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="@+id/connect_button"
                tools:text="DISCONNECTED" />

            <ImageButton
                android:id="@+id/connect_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/connect"
                android:src="@drawable/icon_link"
                android:background="@drawable/round_corner_primary"
                android:onClick="@{() -> clickListener.onConnectClick(device)}"
                setVisibleWhenDisconnected="@{deviceStates.connectionStates.get(device.address)}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageButton
                android:id="@+id/disconnect_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/disconnect"
                android:src="@drawable/icon_disconnect"
                android:background="@drawable/round_corner_dim"
                android:onClick="@{() -> clickListener.onConnectClick(device)}"
                setVisibleWhenConnected="@{deviceStates.connectionStates.get(device.address)}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{device.name}"
                android:textSize="@dimen/large_text"
                app:layout_constraintTop_toBottomOf="@id/connection_state"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginTop="@dimen/margin_small"
                tools:text="Device one" />

            <TextView
                android:id="@+id/battery_level"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                setBatteryLevelText="@{deviceStates.batteryStates.get(device.address)}"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/name"
                android:layout_marginTop="@dimen/margin_large"
                tools:text="Battery level: 83%" />

            <TextView
                android:id="@+id/signal_strength"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                setSignalStrengthText="@{deviceStates.signalStrength.get(device.address)}"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/battery_level"
                tools:text="Signal strength: 83" />

            <ImageButton
                android:id="@+id/delete_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/delete_device"
                android:drawableStart="@drawable/icon_delete"
                android:onClick="@{() -> clickListener.onDeleteClick(device)}"
                android:src="@drawable/icon_delete"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            <ImageButton
                android:id="@+id/settings_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/settings"
                android:drawableStart="@drawable/icon_settings"
                android:src="@drawable/icon_settings"
                android:layout_marginStart="@dimen/margin_small"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toEndOf="@id/delete_button"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/alarm_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:onClick="@{() -> clickListener.onAlarmClick(device)}"
                android:text="@string/alarm"
                app:layout_constraintTop_toBottomOf="@id/signal_strength"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>